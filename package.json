{
  "name": "pi-collapse",
  "version": "0.1.0",
  "description": "Agent-directed, archived range compaction for pi",
  "type": "module",
  "keywords": ["pi-package"],
  "files": ["src", "README.md", "docs"],
  "pi": { "extensions": ["./src/index.ts"] },
  "scripts": {
    "test": "node --import tsx --test test/*.test.ts",
    "typecheck": "tsc --noEmit"
  },
  "peerDependencies": {
    "@earendil-works/pi-coding-agent": "*",
    "@earendil-works/pi-agent-core": "*",
    "typebox": "*"
  },
  "devDependencies": {
    "@earendil-works/pi-coding-agent": "0.85.1",
    "@types/node": "^22.0.0",
    "tsx": "^4.20.0",
    "typescript": "^5.9.0",
    "typebox": "^1.0.0"
  }
}
